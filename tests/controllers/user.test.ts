/**
 * I did not write tests for this controller as I had limited time available to me for the assessment
 */

/**
 * A simple placeholder test
 */
describe('Test user.ts', () => {
    test('Placeholder', () => {
        expect(2 + 2).toBe(4);
    });
});
